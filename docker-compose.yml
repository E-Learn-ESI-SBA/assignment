version: '3.8'

services:
  # app:
  #   build:
  #     .
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - db


  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: madaurus
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: assignment
    ports:
      - "5432:5432"
